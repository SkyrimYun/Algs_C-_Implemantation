#include <iostream>
#include <vector>
#include <algorithm>
#include <sstream>
using namespace std;

int main()
{
    int n;
    cin.unsetf(ios::dec);
    // cin.unsetf(ios::oct);
    // cin.unsetf(ios::hex);
    while (cin >> n)
    {
        cout << n << endl;
    }
}
